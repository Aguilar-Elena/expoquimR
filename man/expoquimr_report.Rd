% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{expoquimr_report}
\alias{expoquimr_report}
\title{Generate an occupational chemical exposure risk assessment report}
\usage{
expoquimr_report(
  coshh = NULL,
  inrs = NULL,
  une689 = NULL,
  evaluator = "",
  workplace = "",
  output = "expoquimr_report.html",
  lang = getOption("expoquimR.lang", "en"),
  open = TRUE
)
}
\arguments{
\item{coshh}{A \code{data.frame} returned by \code{\link[=coshh_evaluate]{coshh_evaluate()}} or
\code{\link[=coshh_from_excel]{coshh_from_excel()}}. If \code{NULL} (default), the COSHH section is omitted.}

\item{inrs}{A \code{data.frame} returned by \code{\link[=inrs_evaluate]{inrs_evaluate()}} or
\code{\link[=inrs_from_excel]{inrs_from_excel()}}. If \code{NULL} (default), the INRS section is omitted.}

\item{une689}{A list returned by \code{\link[=une689_from_excel]{une689_from_excel()}} or constructed
manually with elements \verb{$preliminar} and optionally \verb{$aditivo}. If \code{NULL}
(default), the UNE-EN 689 section is omitted.}

\item{evaluator}{Character. Name of the person responsible for the
assessment. Displayed in the report header. Default \code{""}.}

\item{workplace}{Character. Name or description of the workplace or
workstation assessed. Default \code{""}.}

\item{output}{Character. Path and filename for the output HTML file.
Default \code{"expoquimr_report.html"} in the current working directory.}

\item{lang}{Character. Language for the report body: \code{"en"} (English,
default) or \code{"es"} (Spanish). See \code{\link[=expoquimr_lang]{expoquimr_lang()}}.}

\item{open}{Logical. Whether to open the report in the default browser
after rendering. Default \code{TRUE}.}
}
\value{
Invisibly returns the path to the generated HTML file.
}
\description{
Renders a self-contained HTML report from the results of one or more
assessment methods implemented in expoquimR. Each method section is
included only when the corresponding argument is supplied. The report
includes input data, result tables, density plots (UNE-EN 689), additive
effect groups, and a citation block for the package.
}
\examples{
\dontrun{
# COSHH only
res <- coshh_evaluate(
  nombre = "Toluene", frases = "H315, H336",
  cantidad = "Medium", es_liquido = TRUE,
  t_ebullicion = 111, t_proceso = 20
)
expoquimr_report(
  coshh     = res,
  evaluator = "Dr. Jane Smith",
  workplace = "Printing workshop A",
  output    = "report_coshh.html"
)

# All three methods
ruta <- system.file("plantillas", "plantilla_une689.xlsx",
                    package = "expoquimR")
res_une <- une689_from_excel(ruta)

expoquimr_report(
  coshh     = res,
  une689    = res_une,
  evaluator = "Dr. Jane Smith",
  workplace = "Printing workshop A",
  output    = "full_report.html",
  lang      = "en"
)
}

}
